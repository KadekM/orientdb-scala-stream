//CREATE DATABASE remote:localhost/test root admin plocal

test.tck {
  memory-db = "memory:testdb"

  remote-db = "remote:localhost/test"
  user = "root"
  password = "admin"

  max-elements-from-publisher = 1000000
}

